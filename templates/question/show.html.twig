{% extends 'base.html.twig' %}

{% block body %}
    <h1>
        {{ question }}
    </h1>

    <div>
        Eventually, the question will be printed here
    </div>

    <h2>Answers ({{ answers|length }})</h2>

    <ul>
        {% for answer in answers %}
            <li>{{ answer }}</li>
        {% endfor %}
    </ul>
{% endblock %}
